# Supabase - use service role key only on server (never expose to client)
NEXT_PUBLIC_SUPABASE_URL=https://your-project.supabase.co
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key

# Optional: Supabase anon key is NOT needed for this app (no client-side Supabase)
